# SpeltEngine-AssetHeaderGenerator
This is a tool meant to use for SpeltEngine. It can convert binary files to c-header files so they can be compiled into the source instead of being included from the outside.
